<UserControl x:Class="PdfFlipBook.Views.Controls.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PdfFlipBook.Views.Controls"
             xmlns:pages="clr-namespace:PdfFlipBook.ViewModel.Pages"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:color="clr-namespace:PdfFlipBook.Views.Controls.Color"
             xmlns:helper="clr-namespace:PdfFlipBook.Helper"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance pages:SettingsPageViewModel}">
    <UserControl.Resources>
        <ControlTemplate x:Key="CancelButton" TargetType="Button">
            <Border Background="Transparent">
                <Image Source="../../Images/Cancel.png" Width="57" />
            </Border>
        </ControlTemplate>
        <ControlTemplate x:Key="CorrectButton" TargetType="Button">
            <Border Background="Transparent">
                <Image Source="../../Images/Correct.png" Width="57" />
            </Border>
        </ControlTemplate>
        <ControlTemplate x:Key="EditButton"
                         TargetType="Button">
            <Border Background="{TemplateBinding Background}"
                    Style="{StaticResource SettingBorderStyle}">
                <TextBlock Style="{StaticResource SettingsButtonText}"
                           Text="{TemplateBinding Content}"
                           FontSize="50"/>
            </Border>
        </ControlTemplate>
        <Style TargetType="Border" x:Key="RadioButtonBorderStyle">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="32" />
            <Setter Property="Margin" Value="6 0 6 0 " />
            <Setter Property="Width" Value="819" />
            <Setter Property="Height" Value="125" />
            <Setter Property="Background" Value="#7F7F7F" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsChecked}"
                             Value="False">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                    To="#E4E4E4" Duration="0:0:0.5" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                    To="#7F7F7F" Duration="0:0:0.5" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="RadioButton" x:Key="SettingsRadioButtonsStyle">
            <Setter Property="Foreground" Value="#808080" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Style="{DynamicResource RadioButtonBorderStyle}"
                                x:Name="BorderRadio">
                            <TextBlock Style="{DynamicResource SettingsButtonText}"
                                       FontSize="50"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{TemplateBinding Foreground}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="Foreground.(SolidColorBrush.Color)"
                                    To="White" Duration="0:0:0.3" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="Foreground.(SolidColorBrush.Color)"
                                    To="#808080" Duration="0:0:0.3" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Style="{DynamicResource SettingBorderStyle}" Opacity="0.4"
                Width="1982"
                Height="1820" >
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDown">
                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Border>
        <StackPanel VerticalAlignment="Center"
                    Width="1726"
                    HorizontalAlignment="Center">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDown">
                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <Border Style="{DynamicResource SettingBorderStyle}" Height="226"
                    Margin="0 0 0 16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Text="Время бездействия"
                        Margin="32"
                        Style="{DynamicResource SettingsTextBlock}" />
                    <TextBox Template="{DynamicResource InactivityTextBox}"
                             Margin="600 0 0 0" Width="500" Height="128"
                             FontSize="50"
                             Foreground="{StaticResource WhiteColorForeground}"
                             Text="{Binding PageSettings.InactivityTime,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             TextAlignment="Center"
                             Tag="{Binding SettingsModel.InactivityTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="GotFocus">
                                <b:InvokeCommandAction Command="{Binding GotFocusCommand}" CommandParameter="Inactivity" />
                            </b:EventTrigger>
                        </b:Interaction.Triggers>
                    </TextBox>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource SettingBorderStyle}" Margin="0 16"
                    Height="226">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Text="Интервал перелистывания страниц"
                        Style="{DynamicResource SettingsTextBlock}"
                        Margin="32" />
                    <TextBox Template="{DynamicResource InactivityTextBox}"
                             Margin="180 0 0 0" Width="500" Height="128"
                             Text="{Binding PageSettings.Interval,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             Tag="{Binding SettingsModel.IntervalSwitchPage, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             Foreground="{StaticResource WhiteColorForeground}"
                             FontSize="50" >
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="GotFocus">
                                <b:InvokeCommandAction Command="{Binding GotFocusCommand}" CommandParameter="Interval" />
                            </b:EventTrigger>
                        </b:Interaction.Triggers>
                    </TextBox>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource SettingBorderStyle}" Margin="0 16"
                    Height="226" Padding="32">
                <Grid>
                    <TextBlock
                        Text="Задний фон" HorizontalAlignment="Left"
                        Style="{DynamicResource SettingsTextBlock}"
                        Margin="32" />
                    <Button Content="Изменить" HorizontalAlignment="Right" 
                            Width="500" Height="125" Foreground="White" Background="#5C5C5C"
                            Template="{StaticResource EditButton}" FontSize="50"
                            Command="helper:CommonCommands.NavigateCommand"
                            CommandParameter="{x:Static helper:PageTypes.SelectBackground}"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource SettingBorderStyle}"  Width="1726" Height="285"
                    Padding="32" Margin="0 16 0 16">
                <Grid>
                    <TextBlock Text="Цвет жанров" Style="{StaticResource SettingsTextBlock}" 
                               VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <Border Height="50" Width="100"
                            Background="{Binding SelectedBrush, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0 0 0 10" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Style="{StaticResource SettingBorderStyle}" BorderBrush="DarkGray"
                            BorderThickness="4" Width="500" Height="128">
                        <Grid>
                            <Button Template="{DynamicResource CancelButton}"
                                    HorizontalAlignment="Left" Margin="32 0 0 0"
                                    Command="{Binding EditSettingsCommand}" CommandParameter="Cancel"/>
                            <TextBlock  Foreground="#808080"  VerticalAlignment="Center" FontSize="50"
                                       TextAlignment="Center" HorizontalAlignment="Center" Text="{Binding SelectedColor,
                                UpdateSourceTrigger=PropertyChanged}"
                                       FontFamily="{StaticResource Montserrat}"/>
                            <Button  Template="{DynamicResource CorrectButton}"
                                     HorizontalAlignment="Right" Margin="0 0 35 0 "
                                     Command="{Binding EditSettingsCommand}" CommandParameter="Edit"/>
                        </Grid>
                    </Border>
                    <color:ColorPicker SelectedBrush="{Binding SelectedBrush,
                        UpdateSourceTrigger=PropertyChanged}"
                                       SelectedColor="{Binding SelectedColor, 
                        UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Border>

            <Border Style="{DynamicResource SettingBorderStyle}" Height="306" Margin="0 16 0 16">
                <StackPanel>
                    <TextBlock
                        Text="Схема оформления"
                        Style="{DynamicResource SettingsTextBlock}"
                        HorizontalAlignment="Left" Margin="32 32 0 16" />
                    <StackPanel Orientation="Horizontal" Height="149" Width="1662" Background="{StaticResource DarkThemes}" 
                                Margin="12">
                        <RadioButton Style="{DynamicResource SettingsRadioButtonsStyle}"
                                     Tag="Светлая" >
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="GotFocus">
                                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </RadioButton>
                        <RadioButton Style="{DynamicResource SettingsRadioButtonsStyle}" 
                                     Tag="Тёмная">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="GotFocus">
                                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{DynamicResource SettingBorderStyle}" Height="306" Margin="0 16 0 0 ">
                <StackPanel>
                    <TextBlock
                        Text="Действие после окончания книги"
                        Style="{DynamicResource SettingsTextBlock}"
                        HorizontalAlignment="Left" Margin="32 32 0 16" />
                    <StackPanel Orientation="Horizontal" Height="149" Width="1662" Background="{StaticResource DarkThemes}" 
                                Margin="12">
                        <RadioButton Style="{DynamicResource SettingsRadioButtonsStyle}"
                                     Tag="Начать заново" 
                                     IsChecked="{Binding SettingsModel.Repeat,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="GotFocus">
                                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </RadioButton>
                        <RadioButton Style="{DynamicResource SettingsRadioButtonsStyle}" 
                                     IsChecked="{Binding SettingsModel.NextPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="Следующая книга">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="GotFocus">
                                    <b:InvokeCommandAction Command="{Binding LostFocusCommand}" CommandParameter="Interval" />
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>